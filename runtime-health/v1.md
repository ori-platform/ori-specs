# Runtime Health Socket RPC v1

> Status: Superseded by [v2](v2.md), and still accurate for every released
> version up to and including `2.4.x`. v2 removes `evidence.artifact_version`
> and is what the runtime serves from the following release.
> Source of truth: `ori/runtime_health_socket.py`, `ori/runtime.py`

Read-only runtime health RPC over Unix domain socket for local CLI diagnostics.

## Transport

- Protocol: AF_UNIX stream socket
- Default path: `/run/ori/health.sock`
- Dev fallback path: `/tmp/ori-health.sock` when `/run/ori/health.sock` is not writable
- Default mode: `0o660`
- Security model: local file permissions (no network listener)

## Request

Accepted request payloads:

- `GET_HEALTH`
- empty payload

Maximum request bytes: `1024`.

## Response Envelope

Success:

```json
{
  "schema_version": 1,
  "ok": true,
  "health": { "...": "..." }
}
```

Failure:

```json
{
  "schema_version": 1,
  "ok": false,
  "error": {
    "code": "unsupported_request",
    "detail": "send GET_HEALTH or empty request"
  }
}
```

## Health Payload

| Field | Type | Notes |
|---|---|---|
| `device_id` | `string` | Runtime device ID |
| `uptime_s` | `float` | Runtime uptime seconds |
| `health_socket_path` | `string` | Bound socket path (may use local fallback in dev) |
| `capability_posture` | `object` | Current capability snapshot |
| `sensors` | `array` | Sensor connectivity and staleness snapshot |
| `last_alert_timestamps` | `object` | Alert timestamp maps by channel and trigger |
| `device_policy` | `object` | Policy state snapshot including `enabled` and version/expiry fields |
| `alert_outbox` | `object` | Queued alert retry posture and config |
| `remote_command_lockout` | `object` | Advisory sender risk state when available. Gateway site-health must not expose sender keys. |
| `gateway_broker_posture` | `object` | MQTT broker hardening attestation/diagnostic posture. |
| `state_store_encryption` | `object` | SQLite-at-rest filesystem posture. |
| `evidence` | `object` | Evidence chain signing state (see `evidence/v1.md` in this repo). |
| `commissioning` | `object` | Commissioned safety binding in force and the last verdict (see `commissioned-safety-binding/v1.md`). |

### `capability_posture` object

| Field | Type |
|---|---|
| `available` | `bool` |
| `sms_available` | `bool` |
| `whatsapp_available` | `bool` |
| `gateway_reachable` | `bool` |
| `local_slm_loaded` | `bool` |
| `relay_connected` | `bool` |
| `internet_available` | `bool` |
| `checked_at_ms` | `int` |
| `expires_at_ms` | `int` |
| `gateway_last_heartbeat_ms` | `int \| null` |

### `sensors[]` object

| Field | Type |
|---|---|
| `id` | `string` |
| `type` | `string` |
| `protocol` | `string` |
| `poll_interval_ms` | `int` |
| `connected` | `bool` |
| `last_seen_ms` | `int \| null` |
| `stale` | `bool` |

### `alert_outbox` object

| Field | Type |
|---|---|
| `backlog_count` | `int` |
| `oldest_queued_original_ts` | `int \| null` |
| `oldest_queued_age_ms` | `int \| null` |
| `retry_interval_minutes` | `float` |
| `max_non_tier_d_attempts` | `int` |
| `tier_d_critical_warning_threshold` | `int` |
| `batch_size` | `int` |

### `remote_command_lockout` object

Sender risk state for remote-command abuse. Development deployments may keep
enforcement disabled for observation; production posture requires lockout
enforcement so critical-risk senders are blocked before actuator-affecting
remote commands run.

| Field | Type |
|---|---|
| `enforcement_enabled` | `bool` |
| `risk_window_ms` | `int` |
| `stale_after_ms` | `int` |
| `incident_sender_limit` | `int` |
| `senders` | `object[]` |

Each `senders[]` item includes:

| Field | Type |
|---|---|
| `channel` | `string` |
| `from_number` | `string` |
| `risk_level` | `string` |
| `locked_out` | `bool` |
| `enforcement_enabled` | `bool` |
| `incident_count` | `int` |
| `rejection_count` | `int` |
| `window_ms` | `int` |
| `checked_at_ms` | `int` |
| `reason` | `string` |
| `stale` | `bool` |

### `gateway_broker_posture` object

| Field | Type | Notes |
|---|---|---|
| `available` | `bool` | Whether posture data is available. |
| `gateway_enabled` | `bool` | Runtime gateway integration enabled. |
| `deployment_check` | `string` | `warning` \| `required`. |
| `anonymous_access` | `string` | `unknown` \| `disabled`. |
| `acl_policy` | `string` | `unknown` \| `per_device_required`. |
| `require_credentials` | `bool` | Whether credentials are required by declared posture. |
| `credentials_configured` | `bool` | Whether broker URL includes username/password. |
| `requires_acl_hardening` | `bool` | Whether posture indicates missing/unknown ACL hardening. |

### `state_store_encryption` object

| Field | Type | Notes |
|---|---|---|
| `available` | `bool` | Whether posture data is available. |
| `mode` | `string` | `disabled` \| `filesystem_required`. |
| `satisfied` | `bool` | Whether the configured posture is satisfied. |
| `marker_configured` | `bool` | Marker file was configured. |
| `path_prefix_configured` | `bool` | DB path is under an encrypted path prefix. |

### `evidence` object

Evidence chain signing state. Present whether or not evidence is enabled;
`enabled: false` snapshots carry the disabled defaults.

| Field | Type | Notes |
|---|---|---|
| `enabled` | `bool` | Runtime `evidence.enabled` config. |
| `available` | `bool` | Chain artifact loaded, key open, protocol verified. |
| `public_key_hex` | `string` | Device verification anchor (`''` when unavailable). Not a secret. |
| `artifact_version` | `string` | Version of the loaded chain artifact (`''` when unavailable). |
| `protocol_version` | `string` | The public evidence contract version (`evidence.v1`, see [evidence/v1.md](../evidence/v1.md)) while evidence signing is available; `''` otherwise. The loaded artifact's declared identifier remains internal to runtime validation. |
| `action_event_type` | `string` | Chain event type used for new Tier C/D attestations; `''` while signing is unavailable. |
| `chain_head_hash` | `string \| null` | Current chain head; `null` when unavailable. |
| `pending_export_count` | `int \| null` | Chain rows not yet exported; `null` when unavailable. |
| `last_attested_action_id` | `int \| null` | Highest `action_log` id with a signed attestation. |
| `attestation_gap_count` | `int` | Tier C/D action rows with attestation status `pending` or `failed`. |
| `status_counts` | `object` | Row counts keyed by attestation status. |

### `commissioning` object
The commissioned safety binding the runtime holds, per
[commissioned-safety-binding/v1.md](../commissioned-safety-binding/v1.md).
Present whether or not a binding exists; a device with no actuating hardware
reports `binding_seq: 0` and an empty `zones` list.

| Field | Type | Notes |
|---|---|---|
| `binding_seq` | `int` | Sequence of the accepted binding in force; `0` when none. |
| `binding_hash` | `string \| null` | Canonical hash of the accepted binding; `null` when none. |
| `anchors_configured` | `bool` | A current commissioning anchor is configured out of band. |
| `zones` | `array` | One object per accepted zone: `zone_id`, `sensor_id`, `actuator` (`kind` and `identity`), `commissioned_mapping`, `proof_method`, `proof_performed_at_ms`. |
| `last_verdict` | `object \| null` | The most recent verification outcome for a presented document: `stage`, `reason` (`accepted` when it passed), `binding_seq` of the document, `at_ms`. `null` when no document has been presented since start. |
| `actuation_licensed` | `bool` | A binding **in force** covers every declared actuator and every zone has **both** proof legs: a demonstrated circuit leg and a proven local-GPIO control leg. Either one missing or `undemonstrated` withholds every actuation through the commissioned seam, the initialisation coil command included. A provisional binding never sets this true. |
| `zones[].availability` | `string` | `available` when the zone's mapping may be commanded, `unavailable` when it may not — an unproven control-path leg, or no binding in force. An unavailable zone is reported as unprotected; it is never reported as protected, because an undriven output preserves whatever controller-loss state the site commissioned rather than a safe one. |
| `actuator` | `object \| null` | The local actuator driven through the commissioned seam, when one is connected: `zone_id`, `gpio_pin`, `active_high`, `binding_seq`, `coil` (`energised` or `de_energised`, read from the driver), and `last_command` (`outcome`, `coil_state`, `level`, `executed`) or `null` before any command. `null` when no declared actuator has an accepted zone. |

Gateway site-health projection may include broker posture, state-store encryption,
and alert outbox posture per runtime node. It must exclude `remote_command_lockout`
sender maps because keys are phone numbers or channel identities.

## Error Codes

Known response error codes:

- `request_too_large`
- `unsupported_request`
- `internal_error`
